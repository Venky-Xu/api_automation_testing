name: "Add a PriceBooks"
request:
    url: /priceBooks
    method: POST
    headers: 
        Authorization: Basic ${ENV(AUTH_TOKEN)}
    json: 
      {
        "name": "addPriceBook",
        "appliedStores":["$storeId"]}
