name: getCoreStores
request:
  url: /api/gql/CoreStores
  method: POST
  headers:
    Accept: application/json
    Content-Type: application/json
    Connection: keep-alive
  json:
    {
      business_name: $business,
      location: $location
    }