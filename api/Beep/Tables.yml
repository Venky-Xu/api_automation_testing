name: getTables
request:
  url: /api/gql/Tables
  method: POST
  headers:
    Accept: application/json
    Content-Type: application/json
    Connection: keep-alive
  json:
    {
      business_name: $business,
      storeId: $store_id
    }