name: "onlineOrders"
variables:
  time: ${get_datetime()}
request:
    url: /api/onlineOrders
    method: POST
    headers:
        storehub-business: ${ENV(STOREHUB_BUSINESS)}
        storehub-registerId: ${ENV(STOREHUB_REGISTERID)}
        storehub-token: ${ENV(STOREHUB_TOKEN )}
    json:
      {
          "status":["paid","accepted","logisticsConfirmed"],
          "storeId":"${ENV(STOREID)}",
          "businessName":"${ENV(STOREHUB_BUSINESS)}",
          "channel":3,
          "lastQRSyncDate":$time
      }