name: "syncUserActions"
request:
    url: /api/syncUserActions
    method: POST
    headers:
      storehub-business: ${ENV(STOREHUB_BUSINESS)}
      storehub-registerId: ${ENV(STOREHUB_REGISTERID)}
      storehub-token: ${ENV(STOREHUB_TOKEN )}
    json:
      {
        "storeId":"${ENV(STOREID)}",
        "registerId":2,
        "userActionLogs":[]
      }